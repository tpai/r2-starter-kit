import navTitle from 'redux/modules/navTitle';
import list from 'redux/modules/list';
import post from 'redux/modules/post';
import pagination from 'redux/modules/pagination';

export default {
    navTitle,
    list,
    post,
    pagination
};
